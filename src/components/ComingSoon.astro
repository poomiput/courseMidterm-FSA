---
interface Props {
  n: number;
  title: string;
  accent: string;
  blurb: string;
}
const { n, title, accent, blurb } = Astro.props;
---

<header class="masthead">
  <p class="eyebrow" style={`color:${accent}`}>FSA · บทที่ {n} จาก 5</p>
  <h1>{title}</h1>
  <p class="deck">{blurb}</p>
</header>

<section>
  <div class="card" style="text-align:center; padding:40px 24px">
    <p style="font-size:.8rem; letter-spacing:.1em; text-transform:uppercase; color:var(--ink-3); margin:0 0 8px; font-weight:700">
      กำลังจะมา
    </p>
    <p style="margin:0 0 4px; color:var(--ink-2); max-width:52ch; margin-left:auto; margin-right:auto">
      บทนี้ยังไม่ได้สรุปจากเอกสารต้นฉบับ — จะทยอยทำทีละบท
    </p>
  </div>
</section>

<footer>
  FSA บทที่ {n} · ยังไม่เผยแพร่เนื้อหาเต็ม
</footer>
